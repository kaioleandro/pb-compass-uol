*** Variables ***
# Auth
${REGISTER}      /auth/register
${LOGIN}         /auth/login
${ME}            /auth/me
${PROFILE}       /auth/profile

# Movies
${MOVIES}        /movies

# Reservations
${RESERVATIONS}        /reservations
${MY_RESERVATIONS}     /reservations/me

# Sessions
${SESSIONS}            /sessions

# Setup (dev only)
${SETUP_ADMIN}         /setup/admin
${SETUP_TEST_USERS}    /setup/test-users

# Theaters
${THEATERS}            /theaters

# Users
${USERS}               /users
